/** @type {import("prettier").Config} */
const config = {
    printWidth: 120,
    bracketSpacing: true,
    singleQuote: true,
    plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
