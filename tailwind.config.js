/** @type {import('tailwindcss').Config} */
export default {
    theme: {
        fontFamily: {
            lexend: ['Lexend Deca', 'sans-serif'],
        },
    },
    plugins: [require('tailwind-scrollbar-hide')],
};
